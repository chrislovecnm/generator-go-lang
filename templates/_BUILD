load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    args = [
        "-build_file_name",
        "BUILD,BUILD.bazel",
        "-proto",
        "disable",
    ],
    build_tags = [
        "integration",
        "debug",
    ],
    command = "fix",
    external = "vendored",
    prefix = "<%= importPath %>",
)